import 'package:flutter/material.dart';


class MagnetomerScreen extends StatelessWidget {
  const MagnetomerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('MagnetomerScreen'),
      ),
      body: const Text('Hola'),
    );
  }
}